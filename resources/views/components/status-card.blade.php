<div class="flex-1 {{ $width }} rounded-lg p-2 border border-white {{ $bg }} backdrop-blur">
    <h1 class="text-gray-100 font-medium">{{ $title }}</h1>
    <span class="text-white text-2xl flex flex-row items-center gap-4">
        <i class="{{ $icon }}"></i>
        <p>{{ $value }}</p>
    </span>
</div>